services:

  # ── Backend ───────────────────────────────────────────────────
  backend:
    build:
      context: ./backend
    container_name: serverctl-backend
    restart: unless-stopped
    ports:
      - "${BACKEND_PORT:-8765}:${BACKEND_PORT:-8765}"
    environment:
      BACKEND_PORT: ${BACKEND_PORT:-8765}
      DASHBOARD_TOKEN: ${DASHBOARD_TOKEN}
      AGENT_TOKEN: ${AGENT_TOKEN}
      PROMETHEUS_URL: ${PROMETHEUS_URL:-http://localhost:9090}
      CORS_ORIGINS: "http://localhost,http://localhost:${FRONTEND_PORT:-8090},http://localhost:5173,${FRONTEND_URL:-}"
      SECRET_KEY: ${SECRET_KEY}
    volumes:
      - ./backend/servers.json:/app/servers.json
      - ./backend/users.json:/app/users.json
      - ./agent/agent.py:/app/agent.py:ro
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:${BACKEND_PORT:-8765}/health"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

  # ── Frontend ──────────────────────────────────────────────────
  frontend:
    build:
      context: ./frontend
      args:
        VITE_API_URL: ""
        VITE_DASHBOARD_TOKEN: ${DASHBOARD_TOKEN}
        BACKEND_PORT: ${BACKEND_PORT:-8765}
    container_name: serverctl-frontend
    restart: unless-stopped
    ports:
      - "${FRONTEND_PORT:-8090}:80"
    depends_on:
      backend:
        condition: service_healthy
